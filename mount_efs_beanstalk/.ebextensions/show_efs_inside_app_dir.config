container_commands:
  01_symlink:
    # /my_efs is the env variable $EFS_MOUNT_DIR
    # ./my_efs is the folder in your app dir
    # at the end '/var/app/current/my_efs/' contains files stored in the efs
    command: ln -s /my_efs ./