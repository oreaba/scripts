container_commands:
  01_symlink:
    command: ln -s /efs ./my_efs
    # /efs is the env variable $EFS_MOUNT_DIR
    # ./my_efs is a folder in your app dir you created
    # at the end '/var/app/current/my_efs/' contains files stored in the efs